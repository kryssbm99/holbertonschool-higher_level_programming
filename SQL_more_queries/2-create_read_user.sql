-- Create database hbtn_0d_2 (if it doesn't already exist)
CREATE DATABASE IF NOT EXISTS `hbtn_0d_2`;

-- Create user user_0d_2 with secure password (replace 'password' with your desired password)
CREATE USER 'user_0d_2'@'localhost' IDENTIFIED BY 'password123';
